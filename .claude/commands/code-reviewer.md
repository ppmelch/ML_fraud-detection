---
description: Delegate a review to the code-reviewer subagent (architecture, correctness, reuse)
argument-hint: <PR number, branch, or "current diff">
---

Use the **code-reviewer** subagent to review the following target and report its
findings (blocking, non-blocking, could-not-verify, verdict).

Target:
$ARGUMENTS
