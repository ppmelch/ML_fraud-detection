---
description: Delegate a task to the git-workflow subagent (branches, commits, PRs, pre-PR gate)
argument-hint: <commit, branch, or open-PR request>
---

Use the **git-workflow** subagent to handle the following task. Report back what
it did and the resulting branch / commit / PR.

Task:
$ARGUMENTS
