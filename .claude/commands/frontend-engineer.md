---
description: Delegate a task to the frontend-engineer subagent (frontend/ HTML, CSS, JS, Leaflet, Plotly)
argument-hint: <what to build or fix in the frontend>
---

Use the **frontend-engineer** subagent to handle the following task. Report back
what it changed and any commands the user still needs to run.

Task:
$ARGUMENTS
