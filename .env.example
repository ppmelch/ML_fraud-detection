# Copy to .env to override. Change ports here, never in docker-compose.yml.

# Host port for the FastAPI backend (container always listens on 8000).
BACKEND_PORT=8000

# Host port for the static dashboard, served by nginx (container listens on 80).
FRONTEND_PORT=8080
